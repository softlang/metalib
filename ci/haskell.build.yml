---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: haskell
    tag: "8.0.2"

inputs:
  - name: metalib

run:
  dir: metalib/haskell/fsml
  path: stack
  args:
    - build
    - --install-ghc
